<section class="py-20 md:py-28 px-6 bg-purple/20">
  <div class="mx-auto max-w-[1200px]">
    <div class="grid md:grid-cols-2 gap-12 md:gap-16 items-center">
      <!-- Left: Visual -->
      <div class="relative">
        <!-- Brand card -->
        <div class="bg-surface rounded-[20px] p-8 shadow-lg">
          <h4 class="font-semibold text-sm text-charcoal-light uppercase tracking-wider mb-6">Your Brand</h4>
          <div class="flex gap-3 mb-6">
            <div class="w-10 h-10 rounded-lg bg-coral"></div>
            <div class="w-10 h-10 rounded-lg bg-charcoal"></div>
            <div class="w-10 h-10 rounded-lg bg-cream-dark"></div>
            <div class="w-10 h-10 rounded-lg bg-purple"></div>
          </div>
          <div class="space-y-2">
            <div class="h-3 w-24 bg-cream-dark rounded-full"></div>
            <div class="h-3 w-32 bg-cream-dark rounded-full"></div>
          </div>
        </div>

        <!-- Arrow -->
        <div class="flex justify-center my-4 md:hidden">
          <svg class="w-6 h-6 text-charcoal-light" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path></svg>
        </div>

        <!-- Generated outputs grid -->
        <div class="grid grid-cols-3 gap-3 mt-4">
          <div class="bg-coral/20 rounded-[12px] aspect-square flex items-center justify-center border-2 border-coral/30">
            <span class="text-[10px] text-charcoal/50 font-semibold text-center px-2">IG Story</span>
          </div>
          <div class="bg-coral/20 rounded-[12px] aspect-[4/5] flex items-center justify-center border-2 border-coral/30">
            <span class="text-[10px] text-charcoal/50 font-semibold text-center px-2">Video Frame</span>
          </div>
          <div class="bg-coral/20 rounded-[12px] aspect-[16/9] flex items-center justify-center border-2 border-coral/30">
            <span class="text-[10px] text-charcoal/50 font-semibold text-center px-2">Email Header</span>
          </div>
        </div>
      </div>

      <!-- Right: Copy -->
      <div>
        <h2 class="font-display text-[2rem] md:text-[3.125rem] leading-[1] tracking-[-0.02em] text-charcoal">
          Always On Brand
        </h2>
        <p class="mt-6 text-lg text-charcoal-light leading-relaxed">
          Define your brand once. Every image, video, and asset Claude creates stays consistent — colors, fonts, style, tone. Your brand DNA is saved and reused across every generation.
        </p>
        <p class="mt-4 text-base text-charcoal-light leading-relaxed">
          Our MCP is packed with creative skills that guide Claude to generate multiple variants using different models, compare them side by side, and pick the best match — automatically. The result: studio-quality, on-brand assets every time. No more off-brand AI slop.
        </p>
        <a
          href="#waitlist"
          class="mt-8 inline-block rounded-full bg-charcoal text-white text-sm font-semibold px-8 py-3 hover:bg-charcoal/90 transition-colors"
        >
          Join the Beta
        </a>
      </div>
    </div>
  </div>
</section>
